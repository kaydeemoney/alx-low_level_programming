#include "main.h"

/**
 * this program computes the absolute value of an integer
 * the int to check
 * Return: the absolute value of integer
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
