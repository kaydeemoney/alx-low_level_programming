#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: Prints single-digit numbers in base 10
 * Return: Always 0 (Success)
 */
int main(void)
{
int digit = 0;
char printchar;
while (digit < 10)
{
digitchar = char(digit);
putchar(digitchar);
digit++;
}

putchar('\n');

 return (0);
}
